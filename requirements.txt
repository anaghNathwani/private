cffi>=1.15
pdfplumber>=0.10
reportlab>=4.0

# OCR support for image-based PDFs
pdf2image>=1.16
pytesseract>=0.3
Pillow>=9.0
numpy>=1.24

# System packages also required (install via apt / brew):
#   tesseract-ocr
#   poppler-utils
